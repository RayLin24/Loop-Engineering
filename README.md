# Loop Engineering — 自主循环工作流框架

一个**零代码、纯提示词 + 文件协议**的自主智能体循环框架。给 LLM 一份标准化任务清单，它自动接管：拆解调度 → 派发执行 → 验收反馈 → 更新状态，循环直至完成。

**快速开始**：见 [LOOP.md](LOOP.md)。本文档解释设计。

---

## 一、设计理念

### 1. 编排智能在提示词，状态可靠性在文件系统

传统 agent 框架把编排逻辑写进代码（LangChain、AutoGPT 式的编排器）。本框架反其道而行：**调度决策交给 LLM 本身**（它比 if-else 更懂"这个任务失败的原因是否值得换个思路重试"），而**可靠性交给文件系统**（state.md 不会失忆，回执不会被上下文压缩吞掉）。于是移植成本降到极限——复制几个 markdown 文件即可，无依赖、无 API key、无运行时。

### 2. 上下文是稀缺资源，按角色隔离预算

长循环工作的头号杀手是上下文窗口耗尽与信息退化。本框架的对策是把上下文当作显式管理的资源：

| 角色 | 上下文预算 | 纪律 |
|---|---|---|
| 主控 | 全循环存活，最稀缺 | 只读清单/状态/回执/裁决，禁止执行任务、禁止读产出物全文、禁止亲自跑验证 |
| Worker | 单任务存活，用完即弃 | 只读自己的简报，任务再复杂也不污染主控 |
| Verifier | 单次验证存活，用完即弃 | 独立核查验收标准，消化构建日志与产出物全文，只交回 ≤30 行裁决 |
| 文件系统 | 无限、持久 | 一切跨轮记忆的唯一载体 |

信息在角色间流动时**强制过窄口径**：主控→Worker 只有简报，Worker→主控只有 ≤60 行的结构化回执，Verifier→主控只有 ≤30 行的裁决。这个"信息漏斗"是刻意的——它逼迫每一层只传递决策所需的最小信息，从结构上杜绝上下文膨胀。

### 3. 假设随时失忆，设计就自然健壮

框架不试图"避免中断"，而是让中断变得无所谓：状态先写后做、每轮落盘、冷启动协议对账续跑。上下文耗尽和人为中断走同一条恢复路径。这借鉴了分布式系统的 crash-only design：与其处理优雅关闭，不如让崩溃恢复成为唯一路径。

### 4. 与业界方案的关系

- **Plan-and-Solve**：全局规划前置——但规划由人类在编写 TASKS.md 时完成（依赖图、验收标准），LLM 不在运行时重新发明计划。经验表明运行时自主规划是 AutoGPT 类系统跑偏的主因。
- **ReAct**：Worker 内部采用思考-行动交替（见 `prompts/worker.md` 工作流程）。
- **Reflexion**：验收失败后，主控写出失败反思注入重试简报，而非原样重发。
- **Orchestrator-Worker**（Anthropic 多智能体研究系统的核心模式）：主从分离 + 结构化交接。

## 二、整体架构

```
┌─────────────────────────────────────────────────┐
│  TASKS.md  任务清单（人类编写, 唯一输入, 只读）      │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  主控 Orchestrator（薄调度层）                     │
│  读状态 → 选就绪任务(依赖检查) → 写简报 → 派发      │
│  → 读回执 → 派 Verifier 验证 → 读裁决             │
│  → 更新 state.md → 下一轮                        │
└───┬──────────────▲───────┬──────────────▲───────┘
    │ briefs/      │ reports/  briefs/    │ verdicts/
    │ <id>.md      │ <id>.md   <id>.verify.md  <id>.md
    │ (简报)        │ (回执)    │ (验证简报)  │ (裁决)
    ▼              │          ▼           │
┌──────────────────────┐  ┌──────────────────────┐
│ Worker × N           │  │ Verifier             │
│ (子代理, 一次性上下文,  │  │ (子代理, 一次性上下文,  │
│  可并行)              │  │  独立核查验收标准)      │
└──────────────────────┘  └──────────────────────┘
        ▲                              │
        └────────── loop/state.md ─────┘
           唯一事实源: 状态表 / 决策日志 / 备忘
           （先写后做 + 每轮落盘 → 任意中断可恢复）
```

## 三、三大核心问题的解法

### 上下文管理

- **状态全外置**：`loop/state.md` 是唯一事实源，新会话读它即可完整接管（`protocol/state-spec.md`）。
- **信息漏斗**：主控不读产出物全文、Worker 不读清单全貌；跨任务信息传递走两条受控通道——直接依赖的"上游回执摘要 → 自动注入下游简报"，与全局性的 `loop/decisions.md`（已验收任务的关键决策汇总, 注入每份简报, 解决依赖链过长时的传递性信息丢失）。
- **主动断点**：主控自感上下文退化时落盘停机，用户一句「按 LOOP.md 继续」无损续跑。

### 任务分配与调度

- **依赖驱动**：清单中声明 `depends_on`，主控每轮扫描"依赖全部 done 且自身未完成"的就绪任务。启动时做环检测。
- **并行安全**：清单中声明 `touches`（允许写入的路径）与可选的 `resources`（独占共享资源, 如端口、lockfile），两者均无重叠且无依赖关系的就绪任务同批并行派发（Claude Code 多子代理；无子代理环境自动降级为串行角色切换）。
- **粒度前置**：任务拆解在编写清单时由人完成（一个任务 = 一个 Worker 一次上下文的工作量），运行时不做自主再拆解，换取可预测性。

### 主从协同

- **纯文件协议**：简报（主控→Worker）、回执（Worker→主控）、裁决（Verifier→主控）是仅有的三条通道，格式见 `protocol/handoff-spec.md`。重试前旧文件归档为 `<id>.attempt<n>.md`，失败历史完整可查。
- **验收不盲信**：Worker 逐条自查 + 独立上下文的 Verifier 逐条核查（跑命令、读产出物、查约束与 touches 越界），主控只依据 ≤30 行的裁决定 done/failed。诚实自查有激励：Worker 自报 ❌ 的首次失败不消耗重试次数。
- **失败闭环**：验收失败 → 主控写反思（以裁决 ❌ 条目与 Verifier 的「修复建议」为依据——Verifier 是唯一读过产出物全文的角色）→ 注入重试简报（≤2 次重试）→ 仍败标记 blocked → 不影响其他任务推进 → 最终汇总升级人工。凭据缺失、清单矛盾、破坏性操作、任务超出单次上下文四类问题立即升级。

## 四、文件一览

| 文件 | 作用 |
|---|---|
| [LOOP.md](LOOP.md) | 框架入口：启动指令、移植三步、人类操作速查 |
| [protocol/task-list-spec.md](protocol/task-list-spec.md) | 任务清单格式：字段、依赖语法、验收标准写法 |
| [protocol/state-spec.md](protocol/state-spec.md) | 状态文件格式、状态机、写入纪律、冷启动恢复协议 |
| [protocol/handoff-spec.md](protocol/handoff-spec.md) | 简报/回执/裁决模板、验收流程、重试与升级规则 |
| [prompts/orchestrator.md](prompts/orchestrator.md) | 主控提示词：铁律、主循环伪代码、派发方式 |
| [prompts/worker.md](prompts/worker.md) | Worker 提示词：ReAct 工作流、铁律、回执要点 |
| [prompts/verifier.md](prompts/verifier.md) | Verifier 提示词：独立验收核查、只读纪律、裁决要点 |
| [examples/TASKS.example.md](examples/TASKS.example.md) | 任务清单示例（个人书签管理网站, 6 任务带依赖） |
| [examples/walkthrough.md](examples/walkthrough.md) | 一轮完整循环演练：简报→执行→回执→验收→落盘 |

## 五、适用边界（诚实版）

- **适合**：可分解为 5–30 个中等粒度任务、验收标准可显式写出的项目——代码开发、文档工程、内容生产、数据整理。
- **不适合**：验收标准无法预先定义的探索性工作；任务间深度纠缠无法划清 `touches` 的重构；需要毫秒级协同的实时系统。
- **信任模型**：框架靠提示词纪律约束 LLM，不是硬沙箱。高风险操作（删除、部署、花钱）应在清单 `constraints` 中显式禁止，并依赖宿主工具（如 Claude Code 权限系统）兜底。
