# 回执: T1
- 结果: 完成
- 自查:
  - src/convert.js 导出 c2f 和 f2c 两个函数 ✅（`module.exports = { c2f, f2c }`，两函数均已实现）
  - `node -e "const{c2f,f2c}=require('./src/convert.js');console.log(c2f(100),f2c(32))"` 输出 212 0 ✅（已实际运行，输出为 `212 0`）

## 产出物
- src/convert.js：摄氏/华氏互转纯函数模块（CommonJS，中文注释，无依赖）

## 关键决策
- 函数为纯数学换算，不做输入校验/取整；非数字输入会返回 NaN，由调用方（CLI 层）负责参数校验
- 返回原始浮点数，不四舍五入（如 f2c(100) ≈ 37.77...），格式化交给下游

## 对下游的提醒
- 接口约定：`const { c2f, f2c } = require('./src/convert.js')`
  - `c2f(c: number): number` 摄氏 → 华氏
  - `f2c(f: number): number` 华氏 → 摄氏
- CommonJS 模块，不能用 import 语法直接引入
- 结果未做精度处理，CLI 展示时如需固定小数位请自行 toFixed

## 遗留问题
无
