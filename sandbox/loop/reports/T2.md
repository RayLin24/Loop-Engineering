# 回执: T2
- 结果: 完成
- 自查:
  - ✅ `node src/cli.js 100C` 输出 `100°C = 212°F`，包含 212（实测）
  - ✅ `node src/cli.js 32F` 输出 `32°F = 0°C`，包含 0（实测）
  - ✅ 无参数及非法参数（如 `abc`）均输出中文用法提示且退出码为 1（实测）

## 产出物
- src/cli.js: CLI 入口，解析 `<数值><C|F>` 参数并调用 convert.js 换算输出

## 关键决策
- 输入用正则 `^(-?\d+(?:\.\d+)?)([cCfF])$` 校验：支持负数、小数，单位大小写不敏感；其余一律视为非法
- 输出格式为 `100°C = 212°F`，结果 toFixed(2) 后再 parseFloat 去除多余尾零（212.00 显示为 212）
- 用法提示打印到 stderr，换算结果打印到 stdout

## 对下游的提醒
- 若下游解析 CLI 输出，格式固定为 `<输入值>°<单位> = <结果>°<单位>`，用 ` = ` 分隔
- 只处理 `process.argv[2]` 一个参数，多余参数被忽略而非报错
- 错误信息在 stderr，管道捕获 stdout 时拿不到用法提示

## 遗留问题
无
