# 终局报告：CLI 温度换算工具

## 完成概况
2/2 任务全部完成，共 3 轮循环，0 次重试，0 个 blocked。

## 产出物索引
- `src/convert.js`（T1）— 摄氏/华氏互转纯函数模块（CommonJS，零依赖）
- `src/cli.js`（T2）— CLI 入口，支持 `100C` / `32F` 形式参数，含中文用法提示

## 关键决策汇总
- 换算函数为纯数学换算，不做校验与取整；校验和格式化职责划归 CLI 层（T1）
- CLI 用正则校验输入，支持负数/小数/大小写单位；结果 toFixed(2) 去尾零；用法提示走 stderr、结果走 stdout、非法输入退出码 1（T2）

## 遗留问题与建议
- 无遗留问题。可选扩展：支持开尔文（K）单位、批量换算。
