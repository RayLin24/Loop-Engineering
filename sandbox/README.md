# sandbox — 早期版本试跑存档

本目录是框架**早期版本**的一次完整试跑存档（CLI 温度换算工具, 2 任务），保留用于展示循环的真实产物形态（`loop/state.md`、简报、回执、FINAL.md）。

**注意**：`sandbox/prompts/` 与 `sandbox/protocol/` 是试跑当时的旧版协议副本，已落后于根目录的现行版本（例如缺少 Verifier 角色、无归档规则、无 decisions.md）。**以根目录的 `protocol/` 与 `prompts/` 为准**，不要参照本目录内的副本移植框架。
